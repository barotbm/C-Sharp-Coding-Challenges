# C# Coding Challenges

This repository hosts individual C# challenge classes in a single class library project.

## Structure

- Each challenge lives in its own class file.
- Shared types (if any) live in separate files.

## Build

Open the folder in VS Code or Visual Studio and build the project.

## Conventions

- Namespace: `CodingChallenges`
- One public class per file
